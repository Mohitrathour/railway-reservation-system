# 🚂 Track & Ride Express - Train Ticket Booking System

A robust console-based Railway Reservation System written in C. This project simulates the logic of booking train tickets, calculating fares based on train types and travel classes, and generating a formatted ticket summary.

## 📝 Features
* **Passenger Management:** Uses C Structures (`struct`) to handle multiple passengers per ticket.
* **Dynamic Pricing:** Implements complex `if-else` logic to calculate fares based on 6 different trains (Rajdhani, Shatabdi, Vande Bharat, etc.) and 4 travel classes.
* **Seat Allocation:** Allows users to choose seat preferences (Upper, Middle, Lower).
* **Formatted Output:** Generates a professional ticket summary upon booking confirmation.

## 🛠️ Tech Stack
* **Language:** C
* **Concepts Used:** Structures, Arrays, Strings, Loops, Conditional Logic.

## 🚀 How to Run
1.  **Clone the repository**
    ```bash
    git clone [https://github.com/your-username/Train-Booking-System.git](https://github.com/your-username/Train-Booking-System.git)
    ```
2.  **Compile the code**
    ```bash
    gcc main.c -o train_system
    ```
3.  **Run the application**
    * On Windows:
        ```bash
        train_system.exe
        ```
    * On Linux/Mac:
        ```bash
        ./train_system
        ```

## 👤 Author
MOHIT SINGH RATHOUR
- GitHub:https://github.com/Mohitrathour
